export const SERVICE_TYPES = Object.freeze({
  tour: 'tour',
  hotel: 'hotel',
  room: 'room',
  flight: 'flight',
  train: 'train',
  combo: 'combo',
})

export const SERVICE_TYPE_VALUES = Object.freeze(Object.values(SERVICE_TYPES))
