{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "nodemon src/server.js",
    "start": "node src/server.js",
    "test": "node --test \"src/tests/*.test.js\"",
    "db:validate-migration": "node src/database/validateMigration.js",
    "db:migrate": "node -e \"require('dotenv').config(); require('./src/database/migrator').runMigrations().then((result) => { console.log(JSON.stringify(result, null, 2)); }).catch((error) => { console.error(error.message); process.exit(1); });\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "helmet": "^8.2.0",
    "morgan": "^1.11.0",
    "pg": "^8.22.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}
